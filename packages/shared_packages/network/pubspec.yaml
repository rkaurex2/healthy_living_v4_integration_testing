name: network
description: "Provides features to perform network related tasks like: making API calls."
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.7.2 <4.0.0"
  flutter: ">=3.29.3"
#resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  injectable: ^2.4.2
  get_it: ^7.7.0
  dio: ^5.2.1
  uuid: ^4.5.1
  jwt_decoder: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.3
  mockito: ^5.4.4
  injectable_generator: ^2.6.1
